{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for BrokeSched",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
